name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Create build directory
      run: mkdir build && cd build
    - name: Cmake
      run: cmake ..
    - name: Build
      run: cmake --build .
    
